def max_min():
	old_list = []
	for num in old_list: 
		new_list = []
		min_value = min(old_list)
		max_value = max(old_list)
		new_list = new_list.append(min_value)
		new_list = new_list.append(max_value)
		return new_list


